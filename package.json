{
  "name": "kutup-hukumdari",
  "version": "1.0.0",
  "description": "Kutup Hükümdarı — 3D kutup ayısı yaşam mücadelesi (Three.js)",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "deploy": "bash scripts/publish-pages.sh",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "test": "node --test tests/"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
